# Analize moldcell.md

- In desktop mode, on the main page, after dragging the active advertisement panel (id="ads-view") from right to left or left to right with the mouse,  
    the click action stops   working.
- In desktop mode, on the main page, when hovering over “Fibra+TV” or “VISA Card,” the text changes color. However, when hovering over “5G,” the text color remains unchanged.
- In desktop mode, on the main page, when changing the language, the hovered text changes to gray. Gray text on a purple background is difficult to read, 
    so the hover color should be changed to a more readable one.
- The language switcher should be replaced or supplemented with a flag icon. At the moment, it is not clear where the user needs to click to change the language.